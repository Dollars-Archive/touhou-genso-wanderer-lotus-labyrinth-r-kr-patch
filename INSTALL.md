# 이상한 환상향 -Lotus Labyrinth R- 한국어 패치 설치 가이드

현재 기준 버전은 **v1.0 (미검수)**입니다.

이 문서는 **Nintendo Switch판** 한국어 패치 설치 방법을 안내합니다.

> [!WARNING]
> **v1.0은 미검수 버전입니다.**  
> 실제 플레이 검수 과정에서 오역, 미번역, 문장 어색함, UI 표시 문제 등이 발견될 수 있습니다.

---

## 0. 지원 버전 확인

- Base Title ID: `0100A7A015E4C000`
- 본편 버전: `v1.0.0`
- Update Title ID: `0100A7A015E4C800`
- 지원 업데이트: **v1.0.1 (1.01)**
- 본편 MD5: `C9B152E3E86A898B6921F12A3D8EDE88`
- 업데이트 MD5: `BBD397BC432A5C9D4FFB9D086B34D22F`

> [!WARNING]
> 반드시 **지원 업데이트 v1.0.1 (1.01)이 반영된 깨끗한 원본**을 사용하세요.  
> 이미 다른 번역 패치나 모드가 적용된 파일은 사용하지 않는 것을 권장합니다.

---

## 1. 패치 다운로드

1. [Releases](https://github.com/Dollars-Archive/touhou-genso-wanderer-lotus-labyrinth-r-kr-patch/releases/latest)를 엽니다.
2. 최신 **Nintendo Switch용 한국어 패치**를 받습니다.
3. 받은 ZIP을 원하는 위치에 **완전히 압축 해제**합니다.

> [!CAUTION]
> ZIP 내부에서 패처를 바로 실행하지 마세요.  
> 반드시 먼저 압축을 완전히 해제한 뒤 실행해 주세요.

---

## 2. Nintendo Switch판 패처 실행

1. 배포 ZIP을 원하는 위치에 **완전히 압축 해제**합니다.
2. 배포 폴더의 **한국어 패처 실행 파일**을 실행합니다.
3. 패처가 요구하는 **지원 버전의 깨끗한 원본 게임 폴더**를 선택합니다.
4. 결과물을 만들 위치를 선택합니다.
5. 배경 이미지 옵션에서 원하는 항목을 선택합니다.
6. 패치 생성을 시작합니다.
7. 원본 검사 → 패치 적용 → 결과 검사가 끝날 때까지 기다립니다.

패처는 지원 원본과 패치 파일을 확인하고, 검증을 통과한 경우에만 Switch용 결과를 생성합니다.

생성 결과에서 실제 설치에 사용하는 것은 `atmosphere` 폴더입니다.

최종 적용 구조:

```text
atmosphere
└─ contents
   └─ 0100A7A015E4C000
      └─ romfs
         └─ ...
```

---

## 3. Nintendo Switch 적용 방법

> [!CAUTION]
> **기존 한국어 패치 위에 그대로 덮어쓰지 마세요.**  
> 오래된 파일이 남아 있으면 새 패치와 충돌할 수 있습니다.

### 방법 A. SD 카드를 PC에 연결해서 설치

1. 실행 중인 게임을 완전히 종료합니다.
2. SD 카드를 분리해서 작업할 경우 Switch 전원을 끈 뒤 SD 카드를 PC에 연결합니다.
3. 아래 기존 모드 폴더가 있다면 **백업 후 삭제하거나 내부를 완전히 비웁니다.**

```text
SD:/atmosphere/contents/0100A7A015E4C000
```

4. 패처가 생성한 결과물 안의 `atmosphere` 폴더를 SD 카드 **최상위 루트**에 복사합니다.
5. 복사가 끝난 뒤 아래 경로를 확인합니다.

```text
SD:/atmosphere/contents/0100A7A015E4C000/romfs/...
```

6. SD 카드를 Switch에 다시 넣고 부팅한 뒤 게임을 실행해 한국어 적용 여부를 확인합니다.

### 방법 B. DBI MTP로 SD 카드 분리 없이 설치

1. 실행 중인 게임을 완전히 종료합니다.
2. Switch에서 **DBI**를 실행합니다.
3. **`Run MTP responder`**를 실행합니다.
4. 데이터 전송이 가능한 USB 케이블로 Switch와 PC를 연결합니다.
5. PC 파일 탐색기에서 **`1: SD Card`**를 엽니다.
6. 아래 기존 모드 폴더가 있다면 백업 후 삭제하거나 내부를 완전히 비웁니다.

```text
1: SD Card/atmosphere/contents/0100A7A015E4C000
```

7. 패처가 생성한 결과물 안의 `atmosphere` 폴더를 **`1: SD Card` 최상위 루트**에 복사합니다.
8. 복사가 끝나면 MTP responder를 종료하고 게임을 실행합니다.

> [!TIP]
> 정상적으로 적용되면 별도 재부팅이 필요하지 않을 수 있습니다.  
> 한국어 패치가 적용되지 않을 때만 본체를 재부팅한 뒤 다시 확인해 보세요.

> [!WARNING]
> `0100A7A015E4C000` 폴더를 SD 카드 루트에 바로 넣는 것이 아닙니다.  
> 반드시 `SD:/atmosphere/contents/0100A7A015E4C000/` 구조가 되어야 합니다.

---

## 4. 배경 옵션

Switch판 패치 적용 중 아래 두 옵션 중 하나를 선택할 수 있습니다.

- **대체 배경 사용**
- **원본 게임 배경 유지**

<img class="background-option-shot" alt="배경 이미지 선택" src="https://github.com/user-attachments/assets/1d282dcf-fcfb-44c6-a810-e8d0469c5939" />

<img class="background-option-shot" alt="배경 이미지 변경 전후 비교" src="https://github.com/user-attachments/assets/6c8bb586-2061-43b2-b0aa-45d6d8dec1c3" />

번역이나 게임 내용에는 차이가 없으며, 나중에 패처를 다시 실행해 변경할 수 있습니다.

---

## 5. 패치가 적용되지 않을 때 확인

1. 게임이 **지원 버전 v1.0.1 (1.01)**인지 확인
2. Base Title ID가 `0100A7A015E4C000`인지 확인
3. 지원 업데이트가 반영된 깨끗한 원본인지 확인
4. 다른 한글패치 또는 모드가 섞인 원본을 사용하지 않았는지 확인
5. 기존 `SD:/atmosphere/contents/0100A7A015E4C000` 폴더를 먼저 정리했는지 확인
6. 결과물의 `atmosphere` 폴더를 SD 카드 루트에 복사했는지 확인
7. 게임을 완전히 종료한 뒤 다시 실행하고, 필요할 때만 본체를 재부팅해 확인

패처가 원본 검사 단계에서 중단된다면 파일을 억지로 수정하기보다 **깨끗한 지원 버전 원본부터 다시 준비하는 것을 권장**합니다.

---

## 6. v1.0 주요 한국어화 범위

- 대사 / 이벤트 / 메뉴 / UI
- 아이템 / 장비 / 스킬 / 도감
- 캐릭터명, 기술명, 던전·지역·시설·NPC 명칭
- 한글 폰트 및 주요 게임 내 텍스트
- 튜토리얼 / 이미지 자산
- 설치 시 대체 배경 / 원본 게임 배경 선택 지원

> [!NOTE]
> v1.0은 **미검수 버전**입니다.  
> 실제 플레이 검수 결과에 따라 이후 번역 및 표시 품질이 추가로 보정될 수 있습니다.

---

## 지원 환경

- **Nintendo Switch:** v1.0.1 (1.01)

---

## 주의사항

- 본인이 소유한 게임에서 준비한 원본을 사용하세요.
- 이미 다른 번역 패치나 모드가 적용된 파일은 사용하지 않는 것을 권장합니다.
- 패처가 지원하지 않는 원본을 억지로 수정해서 적용하지 마세요.
- 본 패치는 팬 제작 비공식 한국어 패치입니다.
- 배포물에는 게임 본편 및 완성된 원본 게임 파일이 포함되지 않습니다.
